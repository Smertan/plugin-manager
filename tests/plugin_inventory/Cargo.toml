[package]
name = "plugin_inventory"
version = "0.1.0"
edition = "2024"

[dependencies]
plugin_manager = { path = "../../plugin_manager" }

[lib]
name = "plugin_inventory"
crate-type = ["lib", "cdylib"]